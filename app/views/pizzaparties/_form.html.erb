<%= form_for(@pizzaparty) do |f| %>
  <% if @pizzaparty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pizzaparty.errors.count, "error") %> prohibited this pizzaparty from being saved:</h2>

      <ul>
      <% @pizzaparty.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
